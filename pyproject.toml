[project]
name = "mitmproxy-mcp"
version = "0.1.0"
description = "MCP server for inspecting mitmproxy traffic"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.27.0",
    "mitmproxy>=12.2.1",
]

[dependency-groups]
dev = [
    "pytest>=8.3.0",
    "pytest-cov>=5.0.0",
]
